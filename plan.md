# Cumora 一阶段实施计划：项目共享文件空间

更新日期：2026-08-29

状态：共享文件服务、Linux 挂载、Agent 任务边界和文件面板已部署到远程 `dev` 并启用。2026-08-29 主机重建后旧业务库未保留，原线上测试项目和文件元数据已丢失；服务已基于 `/workspace` 持久化目录恢复并重新通过隔离 Linux 全链路验证。需要在新库重新完成浏览器上传→在线 Agent 修改→浏览器下载点击验收。

实施提交：`5a9fd90`（`feat: add phase-one shared project files`）。提交已推送到 `origin/codex/project-files-phase-one`，并快进合入及推送 `origin/dev`；远程部署 checkout 同步到该提交。阶段状态及验证记录见第 16 节。

## 1. 目标与阶段边界

为群聊提供由项目持有的共享文件空间，让当前群组中的真人和 Agent 访问同一份项目文件。浏览器提供文件管理及附件保存入口；Agent 在当前 Linux 远程主机上通过受控的真实文件系统挂载访问文件。

一阶段完成项目、群组、文件、权限、容量、版本、回收站及 Agent 任务生命周期的闭环。不能用仅有 API/CLI 的方案替代真实目录，也不能用开放底层磁盘目录的方式绕过这些控制。

二阶段处理 Git 仓库关联、克隆、共享工作树、分支和提交；推送及远端合并继续后置。一阶段本身不自动 clone 仓库。

## 2. 已确认的产品规则

| 事项 | 一阶段规则 |
| --- | --- |
| 文件归属 | 项目持有文件；群聊只挂载项目。项目文件不归某个成员或 Agent 私有。 |
| 挂载关系 | 一个群聊最多挂载一个项目；一个项目同时最多挂载一个群聊。不提供跨群共享。 |
| 文件结构 | 每个项目只有一个共享根目录；支持普通文件、空文件、空目录和多层子目录。 |
| 容量 | 每项目最大 5GB，按实际使用增长，不预分配 5GB 磁盘。 |
| 单文件 | 沿用现有 25MB 限制，与项目总容量分别校验。 |
| 存储位置 | 一阶段使用当前 Linux 远程主机的本地持久化磁盘，不接入 R2。 |
| 丢失与备份 | 用户接受当前验证阶段的主机文件丢失；一阶段不做备份或灾难恢复演练，不以此阻塞实施。列表只展示当前实际存在的文件；失效历史引用返回不可用，不自动找回旧版本。 |
| 浏览器入口 | 群成员可以管理项目文件，并通过聊天附件把文件保存到当前挂载的项目。 |
| Agent 入口 | Agent 和文件服务都在该 Linux 主机；提供可直接 `ls/cd/cat/cp/mv/rm` 的目录，支持 Python、常用文档处理程序及编辑器临时文件后重命名保存。 |
| 并发修改 | 覆盖前检查版本；冲突时明确提示或保留两份，不能静默覆盖。 |
| 修改记录 | 记录修改人或 Agent、修改时间和文件版本。 |
| 删除 | 群成员都可以删除文件；删除先进入回收站，永久清理由管理员执行。 |
| 切换项目 | 管理员切换关联，不搬动文件；共享目录及可访问文件随项目切换。 |
| 任务边界 | 新任务使用新项目；旧任务暂停或结束并处理未保存内容。不得把旧任务的目录悄悄指向新项目。 |
| 稳定路径 | 每个项目有稳定目录，例如 `/projects/<projectId>`；每次任务明确指定项目。 |
| 退出或移除 | 退出群、从群移除、从工作区移除后，撤销后续文件访问。 |
| 解散群聊 | 解除项目挂载，保留项目和文件；不影响已经确定的群聊解散行为。 |
| 历史文件链接 | 保留历史消息中的文件名；下载时重新校验当前权限。旧消息不是永久访问凭证。 |
| 文件类型边界 | 不支持符号链接、硬链接、设备文件；不因文件存在而自动执行脚本或指令文件。 |
| 上下文边界 | 项目目录可访问，但不是 Agent 常驻上下文；只有用户明确指向文件或授权相关任务范围时才读取。 |

### 2.1 不纳入一阶段

- Git 仓库关联与任何自动 Git 操作、多 Agent 代码合并。
- 多群挂载同一项目、跨工作区共享、匿名或公开下载链接。
- 其他机器上的 Agent 挂载、Windows/macOS 文件系统客户端、离线同步。
- 自动全文索引、向量检索、扫描全目录、自动摘要或将项目内容写入 Agent 全局记忆。
- 在线多人协同编辑 Office 文档、通用文档预览、自动格式转换、压缩包自动解压。
- R2 迁移、完整 POSIX 兼容保证、数据库工作目录、依赖安装及构建环境。
- 自动把现有聊天附件、在线富文档或 Agent 私有文件迁入项目。
- 自动备份、异机备份、平台重建后的数据恢复及恢复演练。此范围调整不取消正常运行中的并发版本检查、回收站、配额和权限。

## 3. 实施建议默认值

以下是为实施补齐的建议，区别于第 2 节用户明确确认的规则。若技术验证需要调整，应先更新本节并说明影响，不应隐式改变产品行为。

| 事项 | 建议默认值 |
| --- | --- |
| 管理员定义 | 沿用现有权限：当前工作区 `owner/admin`，操作已挂载项目时还必须属于目标群。首版不新增独立的群管理员角色。 |
| 项目创建 | 新共享项目的创建、挂载、切换和解除挂载只允许管理员；普通成员不通过创建群聊接口绕过挂载权限。 |
| 未挂载项目 | 普通成员失去访问权；当前工作区管理员可从未挂载项目管理入口找到、恢复挂载和管理文件，避免切换或解散后文件无处找回。 |
| 容量单位 | 项目配额以 `5_000_000_000` bytes 表示 5GB；单文件沿用现有常量 `25 * 1024 * 1024 = 26_214_400` bytes，界面应说明该单文件限制实际为 25MiB。 |
| 配额口径 | 当前内容、保留的旧版本、冲突副本、回收站内容均占项目额度；同一内容对象的多处引用不重复计数。不做跨项目去重。 |
| 临时写入 | 上传、编辑临时文件和待提交内容也需要容量预留；不能并发绕过 5GB。另设主机磁盘余量和临时空间上限。 |
| 版本保留 | 初版保留成功覆盖前的版本，不自动永久清除；管理员可清理回收站及历史版本以释放容量。删除或替换不能绕过管理员永久清理规则。 |
| 恢复权限 | 当前群成员可恢复回收站文件；同名时提示另存，不覆盖现有文件。 |
| 冲突策略 | 浏览器提示保留两份或重新加载；Agent 写入优先保留冲突副本并返回明确冲突状态，不自动合并、不强制覆盖。 |
| 无挂载群聊 | 显示未挂载状态；管理员可创建或选择项目，不自动为现有所有群聊创建项目。私聊不提供项目挂载。 |
| 已归档项目 | 不接收新挂载、新写入；访问仍需授权。归档已挂载项目也要处理活动任务，不能绕过切换流程；取消归档不恢复旧任务凭证。 |
| 消息文件引用 | 保存项目 ID、文件 ID、版本 ID 和发送时文件名；默认指向发送时版本，移动或改名不让历史链接误指向其他文件。 |
| 自动清理 | 可清理过期且未提交的上传和无引用临时对象；不得以后台清理名义永久删除已提交的用户文件、回收站或保留版本。 |

验证版上限：32 层目录、5,000 个条目（含回收站/失效条目）、每文件 1,000 个版本、每项目 10,000 个版本记录、64 个写入会话、每个挂载 8 个打开的缓冲句柄。写入会话有效期 15 分钟，幂等记录保留 24 小时（最多 10,000 条）。任务临时根目录为 256MiB tmpfs；写新对象前要求宿主机至少剩余 512MiB 加本次写入大小。上述限制不代表宿主机具有无限项目容量，也不限制既有 Agent 私有 home 的总磁盘占用。

## 4. 现有代码与改动范围

以下为本地源码核对结果，不代表这些能力已在远程环境验证可用。

| 模块 | 当前状态 | 一阶段工作 |
| --- | --- | --- |
| `server/src/db/migrate.ts` | 已有 `projects`、`conversations.project_id`；目前允许多个会话关联同一项目。 | 增加文件、目录、版本、回收站、配额、写入会话、访问租约和事件记录；约束项目独占挂载。 |
| `server/src/api/router.ts` | 有项目列表、创建、修改、归档、挂载及附件接口；项目列表按工作区查询，现有挂载只检查群成员身份。 | 收紧角色与可见性；所有关联写入入口统一校验；新文件接口调用共用领域服务，避免把逻辑全部堆进 router。 |
| `server/src/storage.ts`、`server/src/index.ts` | 普通附件已有本地/R2 存储，本地附件通过 `/uploads` 提供。 | 项目文件使用独立的私有存储和鉴权下载；不得直接复用公开静态路径或通用附件 URL 刷新作为授权。 |
| `agent-fuse/main.go` | 已有私有工作区 FUSE；包含内存写缓冲、短时内容缓存，缺少共享文件所需的完整语义。 | 新增项目挂载能力，补齐二进制、目录、重命名、版本提交、配额、撤权及错误传播；不直接暴露 backing store。 |
| `server/src/agents/runtime/fs-endpoints.ts`、`fs-namespace.ts` | 面向 `agent_workspace` 私有文本文件，目录隐式生成。 | 保留现有用途；共享项目使用独立接口和权限域，不能把项目文件当 Agent 记忆。 |
| `server/src/agents/computer/daemon.ts`、`engine.ts` | 私有 Agent home 和可恢复的长驻引擎会话；一次收件箱处理可能涉及多个项目。 | 显式的项目任务、访问租约、停止与恢复边界；拆分跨项目任务，避免旧会话携带旧项目内容进入新项目。 |
| `src/api/client.ts`、聊天与群组组件 | 已有项目标签、群创建选择项目、附件上传。 | 增加项目文件面板、管理入口、附件保存、冲突及容量提示；桌面和移动端复用行为。 |
| 实时状态与测试 | 已有群组隔离、移除成员、解散群聊及权限测试。 | 延续这些边界，增加文件事件、缓存失效、租约撤销和隔离 Linux 文件系统测试。 |

建议新增后端领域目录 `server/src/project-files/`，集中权限、目录、版本提交、配额、回收站和生命周期服务。浏览器 API 与 Agent 挂载调用同一套服务，不能各实现一套文件规则。

这属于文件服务和 Agent 运行边界的改造，不是只增加一个文件列表页面。先通过技术验证，再根据结果细化工期；不能沿用仅提供 API/CLI 方案的工作量估计。

## 5. 权限与项目生命周期

### 5.1 统一授权规则

- 每次访问验证工作区、项目、当前挂载群、当前群成员关系和请求身份；项目 ID、文件 ID、路径、旧链接本身都不授予访问权。
- 普通成员可读写、创建目录、移动、更名、删除和恢复当前群项目文件；不能挂载切换、永久清理或修改配额。
- Agent 必须属于目标群，且持有服务端签发的有效任务租约；不继承调用人的管理员权限。首版永久清理由真人管理员执行。
- 已挂载项目不能仅凭工作区成员或工作区管理员身份绕过群成员校验。未挂载项目的管理员恢复入口是单独、可审计的权限路径。
- 未授权者不能通过列表、搜索、容量、文件名、版本、回收站、缩略图、下载或实时事件发现其他群项目内容。
- 归档、工作区成员移除、群成员移除、主动退群、Agent 退出或停用、群解散都使用相同的租约撤销和任务停止机制。
- 旧凭证在重新入群、重新挂载或服务重启后不自动恢复；服务端必须重新签发。

### 5.2 独占挂载与历史数据

- 服务端和数据库共同保证一个群最多一个项目、一个项目最多一个群；并发挂载时只能一个成功。
- 群创建、已有挂载接口、导入或其他写入 `project_id` 的路径必须使用同一个校验服务，不能只修补一个按钮。
- 安装唯一约束前检查已有多群关联、私聊关联、跨工作区关联等数据；输出冲突清单，由管理员选择保留关系。不能自动解除现有关系或搬动文件。
- 群解散先撤销文件访问与运行任务，再按既有逻辑处理群聊；项目及文件不随群删除。

### 5.3 切换项目状态流程

1. 校验管理员、群成员关系、新项目可用性和独占挂载条件；记录带幂等 ID 的切换请求。
2. 将该群标记为切换中，停止派发新任务，冻结新的文件写入会话；展示仍在运行的旧任务。
3. 协调旧任务停下；对已经开始且仍有权限的写入进行有界收尾，成功内容只提交到旧项目。存在冲突或未保存草稿时明确记录，不静默丢弃。
4. 撤销旧租约、停止引擎及其子进程，确认任务不能继续访问。只做懒卸载或仅给进程发送一次信号不算完成。
5. 事务提交新关联及关联版本，发出范围受限的事件；新任务凭新租约访问新项目稳定路径。
6. 失败或超时保留可恢复状态，告知管理员原因；不能 UI 显示已切换而后台旧任务仍能写入。失败回退也不能复活已撤销的租约。

权限移除与主动项目切换不同：移除立即撤权，不能为被移除者继续开放写入宽限。已经收到但未提交的字节如需保留，只能由受信服务封存为旧项目的受限恢复草稿，并受配额与临时空间限制；不能继续接收被移除者的数据。尚在程序内存、没有交给文件服务的内容不能承诺自动找回。

## 6. 存储、元数据与一致性

### 6.1 私有磁盘存储

- 配置独立、持久化的数据根目录；当前 Grok Bot 主机计划使用 `/workspace/cumora-data/project-files`（依据见第 15 节），部署时显式配置，不放在源码部署目录、临时目录或公开 `/uploads` 下。
- 数据目录只允许文件服务账号读取。Agent 不拥有该账号、主机 root、宿主机控制接口或可绕过挂载的原始路径权限。
- 以服务端生成的项目/内容标识定位对象，不直接把用户提交的路径拼接成宿主机路径。
- 路径规范化后拒绝越界、NUL、非法条目和链接逃逸；文件名按普通数据处理，不能拼进 shell 命令。
- 文件内容按二进制处理，不使用 `agent_workspace.body` 存放 PDF、图片、Office 等文件。
- 项目文件、私有元数据、上传暂存及操作日志可分开存放；这些内部目录不出现在用户的项目根目录里。

### 6.2 数据结构草案

2026-08-29 实现决策：验证版使用 `project_file_spaces.state` JSONB 保存有上限的目录、版本、回收、预留、幂等和事件元数据，以项目行锁串行提交；独占挂载与任务租约使用独立关系表。文件正文仍是私有磁盘对象。此方案减少跨表中间状态，代价是大项目的元数据序列化成本；正式规模化前需测量并评估拆表，不能宣称已通过大容量性能验收。

名称可调整，但需保留以下责任和约束：

| 数据 | 主要内容 |
| --- | --- |
| 项目存储配置 | project/company、配额、已用容量、预留容量、当前挂载状态与关联版本。 |
| 文件和目录条目 | 稳定 ID、project、parent、name、类型、当前内容版本、创建/修改人和时间、删除状态；真实持久化空目录。 |
| 文件版本 | 文件 ID、版本号、私有内容对象 ID、大小、校验值、操作者及可选任务 ID、提交时间。 |
| 回收记录 | 原父目录/名称、删除人/时间、目录删除批次、恢复状态；保留关联文件版本。 |
| 上传与写入会话 | 操作者、项目/关联版本、目标和基准版本、预留容量、暂存对象、到期时间、提交/失败状态、幂等 ID。 |
| Agent 访问租约 | agent、company、conversation、project、task/run、关联版本、有效期和撤销状态。 |
| 操作与事件记录 | 创建、覆盖、冲突、移动、删除、恢复、永久清理、挂载和撤权；不记录凭证或文件正文。 |

项目根不能移动、删除或改成普通文件。目录移动不得形成循环；同一父目录中不能同时存在同名文件/目录；改名和移动时保持历史文件引用可解释。

### 6.3 成功提交的定义

- 写入先进入受控暂存；内容持久化后，在数据库事务内再次检查权限、租约、关联版本、基准文件版本和配额，然后发布新版本及事件。
- 使用内容对象、提交状态和可恢复操作记录协调磁盘与数据库。不能把跨文件系统/数据库的一组操作直接称为天然原子事务。
- 对外只发布完整版本；失败不能把原文件截断、留下半个新文件或让数据库指向不存在的内容。
- 请求重试不重复增加版本、扣额度或创建文件。崩溃恢复能回收未提交暂存并修正预留容量，但不能误删有效历史版本。
- 服务端提交确认和文件系统的 `fsync`、`flush`、覆盖 `rename` 成功，必须对应实际完成的持久提交；不能只入队就报告保存成功。
- 删除进入回收站不释放项目占用；管理员永久清理、物理对象安全清除后才释放相应已用容量。

## 7. 受控文件系统与并发语义

### 7.1 挂载与隔离

- 首选基于现有 Go/FUSE 代码验证项目挂载；是否扩展同一个二进制或独立驱动，以验证结果决定。
- 每个任务使用可区分身份和项目的挂载会话；建议使用受限进程/容器及独立挂载命名空间。仅共享一个 Linux 用户和一个公共挂载点，无法完成可靠的任务归因及撤权。
- 对 Agent 可见路径为 `/projects/<projectId>`；不同任务可以看到同一稳定路径，但服务端始终根据各自租约授权。
- 不使用会自动改指向的 `current-project` 符号链接，不把所有项目原始目录挂给 Agent。
- 现有 Agent persona、私有 home、会话文件和私有记忆继续独立保存；项目目录不替换 Agent home。

### 7.2 首版必须验证的操作

- 目录浏览、`stat`、空文件/空目录、嵌套目录、读取、创建、覆盖、截断、追加、复制、文件及目录更名/移动、删除与恢复。
- `ls/cd/cat/cp/mv/rm`、`mkdir`、`rmdir`、Python 二进制和文本 I/O。
- 编辑器“读取原文件 → 写临时文件 → flush/fsync → rename 覆盖”的完整链路。
- 选取真实的 PDF、DOCX、XLSX 处理工具完成打开、处理和保存测试；不以纯文本样例替代二进制验证，也不承诺所有程序无需适配。
- `rm`、`unlink`、`rmdir` 和递归删除按项目删除规则进入回收站；递归命令可能执行多个操作，失败时应如实显示已完成部分。
- 同项目移动不复制整份数据；跨项目的挂载内移动首版拒绝，不能借移动绕过配额或权限。
- 符号链接、硬链接、FIFO、socket、设备文件及其他未支持对象明确返回错误。不能把禁止的链接悄悄降级成内容不同的普通文件。
- 对 `mmap` 写入、扩展属性等未验证能力明确声明限制并返回可识别错误；不宣称完整 POSIX 支持。

### 7.3 乐观并发控制

- 浏览器更新携带 `expectedVersion`；服务端比较版本成功后才提交。
- 挂载层为一次编辑保留基准版本和独立写缓冲；不能把多个写句柄的缓冲混成一个文件后提交。
- 尤其验证“读文件后关闭，随后通过临时文件覆盖”的情况：基准版本不能到最终 rename 时才读取，否则会漏掉编辑期间的冲突。
- 挂载层需要把任务/编辑会话观察到的目标版本传入提交；对于目标已存在但没有可靠基准的覆盖，拒绝或保存新副本，不能猜测用户意图后覆盖。
- 更新、删除、恢复、目录更名和同名替换的并发都需要检查文件/目录身份及版本，避免删除重建后误认成旧文件。
- 冲突保留现有版本；另存副本也必须通过容量校验，并记录来源、操作者和基准版本。配额不足时明确失败并报告待处理内容，不能虚报副本已保存。
- 无法自动理解任意程序的编辑意图；首版保证无静默覆盖，不承诺自动合并文本、Office 或同时追加的内容。
- 将冲突、超额、撤权、文件不存在等状态映射为明确 API 错误和合适的文件系统错误；错误必须在可返回错误的写入、flush/fsync 或 rename 阶段暴露，不能仅在 release 或后台日志中发现。

### 7.4 缓存与撤权

- 不依赖长寿命 JWT 或前端隐藏入口实现撤权；租约失效和成员变更必须可即时查询。
- 验证内容、目录及内核缓存行为。初版优先关闭会绕过权限检查的缓存，再根据测试优化。
- 已打开文件句柄后续读写、长下载和流式读写也要参与撤销；文件提交前必须再次授权。
- 通过强制停止受控任务及子进程，处理仅卸载目录无法收回的打开句柄与进程状态；后台子进程不得脱离管理后继续访问。
- 已经下载或读入程序/模型内存的内容无法收回；撤权承诺是阻止后续服务访问，不是远程抹除已经取得的副本。

## 8. Agent 调度与上下文

- 每个文件任务绑定 `companyId + conversationId + projectId + bindingVersion + task/runId + agentId`，由服务端确定，不能信任 Agent 自报的项目归属。
- 接入现有即时消息、收件箱、日程和重试路径。排队任务启动前重查当前关联；已运行任务不改绑；旧任务的恢复和重试不能自动迁往新项目。
- 同一次收件箱聚合涉及多个项目时拆成独立任务，不向同一引擎任务开放多个项目以图方便。
- 长驻/可恢复引擎会话按项目与授权边界隔离；切换后不 resume 带旧项目上下文的会话。执行器无法做到这一点时，使用新会话运行文件任务。
- 默认只传必要的项目标识、稳定路径和文件操作规则，不注入目录树、文件正文或自动读取项目中的 `AGENTS.md`、`CLAUDE.md` 等指令文件。
- 上传、挂载、打开群聊不触发 Agent 自动阅读。文件夹中有脚本也不触发执行；用户授权处理特定文件时才按任务调用程序。
- 项目文件不进入现有私有工作区的自动索引/记忆写入链路；项目内容及派生敏感内容不能自动写进跨项目的全局记忆或常驻提示词。
- 群项目停止挂载后，不保留可被其他项目任务复用的文件缓存、目录句柄、凭证或未隔离的任务临时数据。

## 9. 浏览器界面、附件和下载

### 9.1 群项目面板

- 桌面群详情/群头部及移动端群详情提供“项目文件”入口，显示项目名、挂载状态和容量。
- 文件面板包含面包屑、目录列表、文件名、大小、修改人和时间，以及新建目录、上传、下载、更名、移动、删除、回收站和恢复。
- 管理员额外看到创建/选择/切换/解除挂载、历史版本清理和永久清理；危险操作二次确认。
- 切换确认显示目标项目、运行中的旧任务，以及历史链接将重新按权限判断；切换进行中禁用冲突操作。
- 显示无权限、未挂载、切换中、满额、单文件超限、保存冲突、未保存草稿、已删除/已清理等状态。
- 文件内容和文件名按不可信输入处理；首版下载使用安全的内容类型、文件名处理和 `nosniff`，不直接执行或同源展示任意 HTML/SVG。

### 9.2 附件保存与引用

- 上传附件时可选择“保存到当前项目”及目标目录；已有合法可访问附件可执行“另存到项目”。保存是显式动作，不默认收纳所有聊天附件。
- 从项目选择文件发到群里时发送带权限校验的项目文件版本引用，不自动复制到公开附件存储。
- 选择直接保存到项目的新附件，从第一步就走私有项目上传接口，不能先上传到公开 `/uploads` 再复制。
- 保存过程绑定选择时的项目、关联版本和目标目录；过程中项目切换时拒绝或进入明确的旧项目收尾流程，不能自动改存新项目。
- 对已有聊天附件另存，服务端验证源附件及源会话权限，通过内部对象读取；不接受任意外部 URL 作为可抓取来源，防止越权和 SSRF。
- 另存产生独立的项目文件；不默认删除或改变原聊天附件。旧附件已经可下载的副本，不会因为另存为项目文件就被追溯收回。
- Agent 生成的项目文件也通过同一文件 ID/版本引用发回群聊；通用 CLI 附件接口不能成为绕过项目下载检查的代理。

### 9.3 历史链接与实时事件

- 下载/读取每次检查当前授权；不提供可脱离当前权限长期使用的静态 URL。访问已清理版本返回明确的不可用状态。
- 文件移动或改名后仍可按稳定引用找到对应版本；项目切换后消息里的原文件名保留，但无当前项目权限时禁止下载。
- 下载响应禁止公共缓存，支持的 Range/续传同样鉴权；不把短期签名链接当作即时撤权的替代品。
- WebSocket 只向当前授权受众推送变更，不向整个工作区广播项目详情。事件带项目/关联版本，客户端拒绝迟到的旧项目事件。
- 退群、移除、切换、解散和重连时清除相应文件列表、预览、下载状态及请求结果缓存；已在途的旧响应不能重新打开旧项目。

## 10. 实施顺序与完成条件

P0—P3 已有实现及隔离验证，P4 仍在验收。勾选表示该项已有实现和对应检查，不代表整阶段或线上交付完成。历史 P0 记录见第 15 节，当前进展见第 16 节。

### P0：环境预检与关键技术验证

- [x] 只读核实远程 Linux/容器边界、FUSE 可用性、`/dev/fuse`、挂载权限、Agent 实际身份、进程控制方式、持久化磁盘、剩余容量和服务恢复方式。
- [x] 明确文件服务与 Agent 的 OS 隔离方式，验证 Agent 无法直接访问底层对象或其他任务凭证。
- [x] 使用独立测试项目和目录制作最小挂载验证，覆盖真实二进制文件、空目录和编辑器临时文件重命名保存。
- [x] 两个身份并发覆盖同一文件，验证无静默丢失；验证 `rm` 进入回收站。
- [x] 打开文件后撤权，验证后续 I/O、缓存、子进程和旧凭证均不能继续访问。
- [ ] 模拟项目切换，验证旧任务不会写入新项目；用一个真实 Agent 引擎验证会话和项目隔离。
- [ ] 记录支持矩阵、错误语义、容量默认值、进程停止方案和性能基线，更新本文。

完成条件：上述关键行为通过实际 Linux 测试。如果基础隔离、撤权或保存语义做不到，应调整部署/挂载方案后重测，不降低用户已确认的要求，也不提前把界面开发当作核心进展。

### P1：数据结构与共用文件服务

- [x] 完成项目/群关联存量检查及非破坏性迁移方案。
- [x] 实现文件目录、版本、回收站、操作记录、写入会话和配额预留。
- [x] 实现私有磁盘对象、持久提交、幂等重试、无引用对象清理；进程崩溃与磁盘丢失的覆盖仍在扩展。
- [x] 实现统一权限及项目独占挂载服务，覆盖群创建和所有关联入口。
- [x] 提供受鉴权的文件管理、上传、下载、版本、回收站和管理员清理 API。

完成条件：独立数据库/磁盘环境下，权限、并发、容量与崩溃恢复集成测试通过，不依赖 UI 保证正确性。

### P2：项目挂载与 Agent 生命周期

- [x] 将 P0 验证的文件系统方案接入 P1 服务，完成二进制、目录、原子替换和错误传播。
- [x] 接入任务租约、稳定路径、身份审计和缓存撤销。
- [x] 改造 Agent 调度、恢复、日程和重试的项目绑定；保持 persona/home 独立。
- [x] 接入切换、归档、解散、退群及成员/Agent 移除流程，处理子进程和未提交内容。
- [ ] 验证不自动扫描、注入或执行项目内容，不跨项目恢复旧上下文。

完成条件：两个实际 Agent/任务身份和浏览器可以共享同一项目文件；冲突、撤权、切换和错误能端到端验证。

### P3：桌面/移动端与附件闭环

- [x] 实现共享的项目文件面板、目录管理、容量、修改记录、回收站与管理员操作。
- [x] 接入群组挂载管理及切换等待/重试，普通成员保持文件管理权限但不显示管理员操作。
- [x] 实现新附件直接存项目、已有附件另存、项目文件版本发送回群聊。
- [x] 接入文件变更事件、缓存清理、冲突及失败恢复提示，中英文文案齐全。

完成条件：桌面和移动端均能完成挂载、上传、Agent 修改、下载、删除、恢复和切换的完整流程。

### P4：验收、部署与文件丢失处理

- [x] 完成第 11 节测试与权限回归，记录通过项及仍不支持的程序/文件系统操作。
- [x] 数据目录与源码更新、临时目录清理分离；使用隔离测试数据验证实际文件丢失后列表、下载、配额和新上传正常。不要求备份、容器重建后保留数据或恢复演练。
- [ ] 先小范围启用功能开关，使用专门测试群和测试项目验证；不删除、覆盖或解散真实业务数据作测试。
- [x] 验证文件服务、挂载和任务监管在重启后的恢复顺序；重启后旧租约不自动有效。
- [x] 观察容量、暂存泄漏、失败写入、拒绝访问、活动租约和任务停止失败；日志不含密钥或文件正文。
- [x] 交付管理员操作说明、已支持能力、限制、恢复步骤和实际部署记录。

完成条件：可供真实群组使用，失败能够诊断和恢复；所有已确认的一阶段规则均有实现与测试证据。

## 11. 验收矩阵

| 类别 | 必测场景与通过标准 |
| --- | --- |
| 基本共享 | 真人上传后 Agent 可读；Agent 写入后真人可下载；二进制校验一致；空目录、中文、空格、特殊字符及多层目录正确。 |
| 普通成员 | 能管理当前群项目文件及回收站；不能切换项目、永久清理或修改额度；直接调用 API 也不能越权。 |
| 管理员 | 当前群管理员可挂载、切换、解除挂载和永久清理；其他工作区管理员身份不授予本工作区权限。 |
| 隔离 | 猜测 project/file/version/object ID、遍历路径、旧链接、Range、搜索、事件和通用上传接口都不能读取其他群文件。 |
| 独占挂载 | 两群并发挂同一项目只成功一个；创建群聊不能绕过；既有异常关联不被迁移脚本静默改写。 |
| 覆盖冲突 | 两浏览器、浏览器与 Agent、两 Agent、多个写句柄并发修改；至少一个得到冲突，原有效版本与冲突内容不被静默覆盖。 |
| 编辑器保存 | 原文件读取后另一个人提交，再临时文件 rename 覆盖，仍识别冲突；无冲突时正确提交完整文件。 |
| 目录冲突 | 删除/恢复与写入并发、目录移动与子文件写入并发、同名创建、目标删除重建等不会串文件或丢版本。 |
| 配额 | 25MiB 和 5GB 边界；小额度测试模式验证并发预留、覆盖、冲突副本、版本、回收站、稀疏写/截断和暂存都不能绕过限制。 |
| 永久清理 | 普通用户/Agent 的 `rm` 不永久删除；管理员清理受二次确认和授权检查；实际空间释放与统计一致。 |
| 撤权 | 退群、群移除、工作区移除、Agent 停用后，旧下载、打开的句柄、挂载缓存、旧 token 及后台子进程失去后续访问能力。 |
| 切换 | A→B 不搬文件；新任务使用 B；旧 A 任务、已排队任务和断线重试不能误写 B；失败/超时不会伪报切换成功。 |
| 解散与恢复 | 解散群保留项目及内容；管理员能找到未挂载项目；重新挂载需重新授权，旧租约仍无效。 |
| 历史链接 | 文件更名/移动后引用不串到同名新文件；切换后仍显示原文件名，但无权限者不可下载；已清理版本清晰提示。 |
| 上下文 | 挂载/上传不扫描文件；项目指令文件不自动加载；切换后不 resume 旧项目会话，不把项目内容写进全局记忆。 |
| 故障恢复 | 写入中断、磁盘满、文件服务/API/数据库退出、事件丢失、任务强停、挂载重启；已确认提交不丢失，未确认保存不显示成功。 |
| UI/实时 | 桌面、移动端、慢请求和重连；无权限缓存及时清除，迟到响应不恢复旧文件列表或旧项目操作。 |
| 既有功能 | 50 人工作区、群邀请不自动加入 Everyone、同事移除、观测管理员权限、成员编辑权限、解散群、普通附件与 Agent 私有文件继续正常。 |

测试优先扩展现有权限/群组集成测试；新文件服务及挂载行为缺少自然归属时再新增测试文件。真实 FUSE 测试必须在隔离 Linux 环境执行，不能仅用 mock API 或 Windows 单元测试替代。

完整验收命令（实际已运行项目见第 16 节；不能把下面的命令清单当作通过记录）：

```text
npm run typecheck
npm run server:typecheck
npm run build
npm test
npm run test:integration
cd agent-fuse && go test ./...
```

集成测试必须显式配置独立的 `INTEGRATION_DATABASE_URL`、Redis 和文件数据根目录；缺少测试数据库导致 skipped 不算通过。不得使用线上数据库、线上 Redis 或业务文件目录执行清理测试。

## 12. 部署与回滚约束

- 使用独立功能开关，默认关闭；迁移与新接口先兼容部署，通过验证后再向指定群组开放。
- 数据目录和服务权限需预先创建并检查；更改宿主机权限、容器能力或服务启动配置前单独说明，不扩大 Agent 的宿主机权限来省略隔离。
- 按用户最新要求，一阶段不实施备份或灾难恢复；主机文件丢失允许发生。检测不到的文件不作为可下载文件展示，旧引用返回不可用，实际占用重新核算；不得因此扩大 Agent 对底层目录的访问权限。
- 关闭功能或回滚业务代码前，停止新文件任务、撤销租约、处理未提交写入；保留项目数据和新增表，不通过删数据实现回滚。
- 回滚到旧版时必须阻止旧的宽松项目关联接口、创建群聊入口重新修改受保护项目；不能留下新数据却让旧代码绕过权限和独占约束。必要时保留兼容保护层或暂时关闭相关入口。
- 远程现有部署仍是开发服务，且 `agent.md` 记录未使用 systemd 自动启动；文件持久化、服务监管和重启恢复必须实测，不能从源码中存在 Docker/FUSE 配置推断线上已经具备。

## 13. 二阶段：项目共享 Git 仓库

2026-08-30 按最新产品决定调整二阶段模型：一个 Cumora 项目持有一套 Git 用户名和 token，并可配置多个共用该凭据的 HTTPS Git 仓库。不再使用工作区全局凭据、每仓库独立 token 或“同一时间仅一个工作区 token 生效”的规则。token 使用服务端 AES-256-GCM 加密，只在受信 Git 服务 clone 时通过一次性 `GIT_ASKPASS` 使用；不进入 URL、参数、日志、浏览器响应、Agent 环境或仓库配置。

实现边界如下：

- 仓库普通文件进入项目的共享版本文件空间，与共享文档共同计入项目 5GB；单文件仍受 25MiB 限制。界面把“共享文档”和“Git 仓库”分区展示，但两者属于同一项目。
- Agent 在稳定路径 `/projects/<projectId>/Repositories/<仓库名>` 读写共享工作树。仓库不是 Agent 常驻上下文；只有用户明确要求查看或修改代码时才读取，不自动执行仓库里的脚本、hook 或指令文件。
- 群内真人和 Agent 都能浏览及下载工作树。真人没有修改入口，后端也拒绝真人上传、覆盖、移动、重命名、删除或恢复 Git 区域；只有持有当前项目任务租约的 Agent 能修改。
- `.git` 数据库和 token 位于私有 `CUMORA_PROJECT_GIT_ROOT`，Grok Bot 固定为 `/workspace/cumora-data/project-git`。它们不挂给 Agent。Agent 通过受控 `cumora project-git list/status/switch/commit` 操作共享仓库。
- 分支切换是共享状态变化。切换前必须工作树干净，且没有另一条活动 Agent 项目任务；不会把已运行任务目录静默改指向其他项目。提交同样串行化，并记录 Agent 作者和提交时间。
- 第一批支持 clone、浏览、Agent 编辑、切换本地已有分支和本地 commit；不提供 fetch/pull/push、远端合并、PR 或真人在线编辑。已经 clone 的仓库不能由管理员“重新同步”覆盖本地提交。
- 不支持符号链接、硬链接、设备文件和 submodule。工作树使用现有版本检查、冲突副本和回收站规则；服务端 mirror 不作为用户可见文件，也不进入模型上下文。

实施清单：

- [x] 将数据库改为项目级单一加密 token 加多仓库，并移除运行路径中的工作区全局 active token 和每仓库 token。
- [x] 将工作树导入共享文件版本层，共享 5GB 计量；添加 Git 来源标记和真人只读的服务端强制校验。
- [x] 移除每任务私有 checkout；Agent 通过共享 FUSE 路径编辑，并使用受控 Git 状态、切分支和提交命令。
- [x] 将界面改为共享文档与多个 Git 仓库分区；所有群成员可浏览/下载，只有管理员配置仓库。
- [x] 完成独立 PostgreSQL/Redis、真实 HTTPS clone、共享编辑/commit、分支切换、冲突、配额及 Linux FUSE 回归。
- [x] 推送修订后的 `dev`，配置持久 Git root，并部署数据库、API 和界面。
- [ ] 使用管理员提供的真实项目凭据和目标仓库完成线上管理员配置、真人浏览下载及在线 Agent 编辑/commit 点击验收；不为验收伪造生产 token 或仓库。

旧的“一个项目一个仓库、工作区多 token 但仅一个全局生效、每任务私有 checkout”，以及短暂出现过的“每仓库独立 token”实现保留在 Git 历史中，仅作为开发过程记录；它们不再是交付行为，后续 `dev` 以本节规则为准。

## 14. 开始实施时的第一步

当前重点：完成剩余运行时兼容、重启与重复运行检查，再按部署顺序灰度启用；不以界面已完成或一次测试成功替代整体验收。

## 15. 执行记录：P0 初步验证（2026-08-28）

本节保留当日历史状态，不代表 8 月 29 日的实现进展。

### 已完成的工作

- 新增 `npm run project-files:preflight -- --data-root <existing-directory>`，只做环境检查，不安装软件、不使用 sudo、不创建项目目录、不连接业务数据库。工具区分“可进行原型测试”和“可生产使用”，不会因为存在 `/dev/fuse` 就判定整套功能可用。
- 新增 4 项预检测试，覆盖挂载点解析、容器/内存/只读存储、FUSE/命名空间不可用、目录不可写，以及不误报生产就绪。
- 在当前远程主机实际运行预检。内核 `6.12.94+`、Go `1.24.4`；`/dev/fuse` 可打开；普通用户创建 user/mount/PID 命名空间成功。未安装 `fusermount`，原型通过 go-fuse 的 `DirectMountStrict` 在独立 user namespace 内挂载，没有增加宿主机权限。
- 新增并执行 2 项真实 Linux FUSE 挂载测试：两个独立挂载的写句柄使用同一基准版本时，后提交者收到 `ESTALE` 且冲突内容保留；已打开的文件在原型访问许可撤销后，读取、写入、fsync 和目录列举返回 `EACCES`。
- 远程 Go 测试编译和上述 2 项测试通过；本地 4 项预检测试及后端类型检查通过。远程测试只使用独立临时目录，未改变业务文件、群组、数据库或线上 Agent 进程。
- 平台持久化调查后，修正 overlay/未知挂载的判定并扩展为 5 项预检测试，复测全部通过；后端类型检查再次通过。原始远程预检 JSON 保留当时结果，修正原因单独记录；本次没有重新运行远程挂载测试或重启服务。

### 未完成与环境前置条件

- `/workspace` 和 `/home/box` 均位于容器 `overlay` 根文件系统；当前未观察到独立数据卷。可见空闲空间约 118.5GB，但这不能证明容器重建后的持久性，也不等于可独占的项目容量。
- 用户补充主机由 Grok Bot 提供后，已查证官方指定 `/workspace` 保存持久项目文件，正常 Update / Recover 保留持久状态；Reset 可能回退快照并丢失最近未同步内容。因此 overlay 不再是开发阻塞项，预检将其改为 `unverified`，不自动判失败或生产就绪。依据：[计算机与应用](https://docs.x.ai/grok-bot/computer-and-apps)、[故障排查](https://docs.x.ai/grok-bot/troubleshooting)。
- 计划私有数据根采用 `/workspace/cumora-data/project-files`，通过部署配置指定，与 `/workspace/cumora` 源码及公开附件分离；尚未创建或启用。用户随后明确接受文件丢失，取消一阶段备份与灾难恢复前置条件；仍不擅自重置线上主机。
- 当前 API 与 Agent 同属 Linux UID 1000；命名空间创建成功不代表已经完成文件服务和真实 Agent 的隔离。正式任务 runner、私有 backing store 隐藏、子进程监管和会话隔离仍待实现及验证。
- FUSE 测试使用内存测试夹具，不是生产文件服务；尚未验证数据库/磁盘持久提交、真实群成员撤权、原子替换保存、回收站、配额、文档处理程序及真实 Agent 项目切换。因此不能标记整个 P0 或一阶段完成，也不提前启用功能。
- 完整的迁移、文件 API、浏览器面板、附件流程及 P1—P4 尚未实施。

原始预检报告位于 `docs/project-files-p0-preflight.json`；测试边界和运行方法见 `docs/project-files-p0.md`。

## 16. 执行记录：共享文件实现（2026-08-29）

- 已按用户新决定取消备份和灾难恢复要求。磁盘对象丢失后列表隐藏失效文件、旧链接不可用、配额重算；不自动把旧版本恢复成当前文件。管理员可清理失效文件残留历史。
- 新增共用文件服务、独占挂载迁移/触发器、鉴权下载、项目附件引用、真实 FUSE 挂载、任务隔离和停止确认；桌面与移动端文件面板已接入。
- Linux 隔离测试运行于自建临时 PostgreSQL、Redis 和私有目录，未使用或重置业务库。文档依赖仅安装在测试临时目录。测试脚本：`scripts/test-project-files-linux.py`。
- 已实际运行 DOCX、XLSX、PDF 读写和临时文件替换；修复 Excel 的 OPEN 后单独 SETATTR 截断误判冲突。
- 已用现有 Codex 引擎完成项目文件写入并由后端读回校验。其配套 `codex-code-mode-host` 必须一并只读挂入，不能只挂主执行文件。
- 2026-08-29 完整 Linux 单元测试：775 项，772 通过，3 项平台相关跳过；新增领域测试 10 项通过。此前 Windows 全量测试中的 Unix shell 假引擎不兼容，因此完整回归转到 Linux；测试归档按 Linux checkout 规范化换行。
- 项目集成测试已扩展至 20 项：长下载撤权、停止恢复和真实 Codex/Grok 隔离任务均通过。FUSE 偶发 I/O 失败定位为 Go 运行时 `SIGURG` 导致的 `FUSE_INTERRUPT` 取消了 HTTP 请求；按 go-fuse 约定忽略该类中断并继续有界请求后，真实挂载重复 20 轮全部通过，心跳的普通取消语义保持不变。
- 随后增加 25MiB 实际 HTTP 请求边界、daemon 消失后任务命名空间退出、私有数据目录 0700、切换中项目不可由直接 SQL 挂载等验证；本地类型检查及领域测试通过。新增 Linux 集成项需在远程主机恢复后执行，不能提前记为通过。
- 浏览器已检查实际文件面板：桌面 1280×720、手机 390×844、普通成员管理员按钮隐藏、目录导航、失效文件提示，以及管理员查看未挂载项目、返回当前群项目的流程。使用隔离接口数据；尚未进行线上完整点击验收。
- 线上只读检查：1 个项目，未发现多群/跨工作区/私聊异常关联；8 个 Agent 中 5 个 Codex、3 个 Grok。未调整绑定或停掉现有任务。
- 前后端类型检查与构建通过；构建保留既有大包和混合导入警告。
- 实施提交 `5a9fd90` 已推送到实施分支并快进合入 `origin/dev`；远程 `/workspace/cumora` 已同步到同一提交。业务库执行兼容迁移成功，历史关联约束没有报错。
- 正式数据根 `/workspace/cumora-data/project-files` 已创建为 0700，与源码、公开附件和测试目录分离。API 与 daemon 使用同一私密宿主机控制密钥，密钥只存在于 0600 的远程配置文件，不进入仓库、日志或命令参数。
- 远程正式目录预检通过 Linux、`/dev/fuse`、非特权 namespace、目录可写和剩余空间检查；overlay 持久性仍按用户决定视为可接受风险，不宣称有备份或容器重建恢复能力。
- Linux 全量单元测试最终为 776 项：773 通过、3 项平台相关跳过；项目集成测试 22 项全部通过，包含真实 Codex 和 Grok 引擎。`go test ./...`、前后端类型检查及前端构建通过；构建仅保留既有大包和混合导入警告。
- 文档处理依赖安装在独立目录，版本为 python-docx 1.2.0、openpyxl 3.1.5、pypdf 6.0.0、reportlab 4.4.3。正式 Agent 运行环境已加载项目任务二进制、受审运行目录和本机 API。
- 功能开关已在线启用。应用、API 与八个 Codex/Grok Agent 恢复运行；公网健康接口为 200。用户态守护器实测应用子进程退出后约 4 秒恢复、daemon 子进程退出后约 2 秒恢复，恢复后项目任务环境仍完整。
- 线上登录态先确认“测试群组”的项目文件入口、未挂载状态和项目独占选择约束；随后通过正式前端表单创建“共享文件验收”项目。原生项目切换确认框与自动化驱动不兼容，因此使用同一个 `attachGroupProject` 领域服务完成挂载，仍执行管理员、群成员、工作区和独占约束校验，没有直接改表。
- 正式文件服务以当前管理员身份写入并读回 `cumora-online-check.txt`，内容为 31 bytes；网页显示 `31 B / 5.00 GB`、修改人和文件操作。当前浏览器沙箱拒绝把本机文件交给上传控件，也没有捕获网页下载事件；没有绕过限制冒充浏览器上传/下载，更没有在业务群触发 Agent 消息。完整浏览器上传→在线 Agent 修改→浏览器下载点击闭环仍是剩余灰度项。
- 管理员操作、能力边界、恢复命令和部署路径见 `docs/project-files.md`。
- 2026-08-29 较早一次收尾曾因 `bore.pub:3444` 无法返回 SSH banner、Cloudflare 公网入口 530 而暂停，没有在断连状态下改动远程。SSH Cloudflare 域名恢复后从该安全点继续，完成了上述同步、迁移、启用和验证；`bore.pub` 不再作为默认部署入口。
- 同日 Grok Bot 主机约在 UTC 07:14 重建。源码、`.env`、隧道 token 与项目对象目录仍在 `/workspace`，但原 PostgreSQL/Redis、运行进程和系统包消失；重新安装 PostgreSQL 时创建的是全新业务库。旧消息、会话、自定义成员/群组、Computer 和 `project_file_spaces` 元数据没有迁移，磁盘上三个残留对象因无元数据而不在当前列表显示，不能描述为业务数据恢复。
- 新增仓库内 `ops/grok-bot/` 验证主机恢复栈，并部署到 `/workspace/cumora-stack`。PostgreSQL 改用 `/workspace/data/postgres`，Redis 使用 `/workspace/data/redis` 且开启 AOF；Cloudflare token、SSH authorized keys、持久 SSH 主机密钥和当前数据库配对的 Computer 配置保存在权限受控的 `secrets/`，不进入仓库。
- 恢复栈用用户态监督器拉起 Cumora、cloudflared 和项目 daemon，并在子进程退出后重启。`ensure-running.sh` 修复了锁文件描述符继承问题，已验证重复执行不会启动第二个监督器；项目 daemon 也使用独占文件锁，避免重复运行。应用子进程被终止后自动恢复，公网和 SSH 隧道未随之重启。
- 当前容器 PID 1 为 `tini`，没有 systemd 或 cron，仓库脚本不能自行跨越整机重建。仍需 Grok Routine 或人工调用 `/workspace/cumora-stack/bin/ensure-running.sh`；Routine 可能因长期无活动暂停，因此该方案仍只适合验证环境。
- 新库已重新配对当前主机，六个种子 Agent 及重新创建的 `codex` Agent 统一使用 Codex 引擎且 Computer 在线。重建后的隔离 Linux 验证使用临时 PostgreSQL、Redis 和文件目录，预检 5/5、领域测试 10/10、集成测试 20 项通过；默认未调用真实外部引擎的两项测试按设计跳过，业务库和线上目录未被测试修改。
- 项目文件正文根已确认是 `/workspace/cumora-data/project-files`（0700）。任务内 `/projects/<projectId>` 仅是受控 FUSE 挂载，不承载持久化正文；恢复脚本新增真实路径校验，功能开启时若 `CUMORA_PROJECT_FILES_ROOT` 不在 `/workspace` 下则拒绝启动，防止以后误写容器临时层。
- 2026-08-30 二阶段多仓库实现以 `7e75be5` 推送至 `origin/dev` 并部署到 `/workspace/cumora`。隔离 Linux 验证预检 5/5、项目工作区单元测试 12/12、项目集成测试 22/22；覆盖一套加密项目凭据服务多个仓库、真实 GitHub HTTPS clone、共享配额、真人写入拒绝、Agent 编辑和受控 commit、真实 FUSE、撤权隔离及常用文档程序访问。两项需要真实外部模型的测试按设计跳过，未接触业务库。
- 远程数据库已创建 `project_git_access` 与 `project_git_repositories`，部署核验时均为 0 行；没有为验收写入假凭据。功能根为 `/workspace/cumora-data/project-git`（0700，`box:box`），`.env` 已启用项目 Git 并设置独立加密密钥。恢复栈的 `start-deps.sh` 同步了 `/workspace` 路径守卫；公网和本机 `/api/health` 均返回 200。剩余线上点击验收需要真实目标仓库和管理员凭据。
- 2026-08-30 针对主监督器退出后 Cloudflare 1033，恢复栈增加独立 `watchdog.py`：检查监督器、本机 API 和 Cloudflare `/ready`，连续失败时清理登记过的孤儿进程组并调用幂等恢复脚本。监督器同时改为观察仍存活的项目 daemon 单实例，避免父进程被杀后每 5 秒启动一次锁失败的副本。整台 Grok Bot computer 重建仍需外部 Routine 或人工唤醒，不把用户态看门狗描述为系统级开机自启。
- 同日完成监督器 `SIGKILL` 故障演练：看门狗保持原 PID，识别主监督器消失，清理登记的 Cumora 与 cloudflared 进程组并启动新监督器；公网、本机 API、Cloudflare `/ready` 和 SSH 均恢复。恢复后重复运行 `ensure-running.sh` 不改变监督器或看门狗 PID，四类进程各一份、两把独占锁均有效。PID 发现使用 `/proc/<pid>/cmdline` 的完整参数匹配，避免把命令文本中仅包含脚本路径的 shell 误判成目标进程。
